package org.wgcodecompany.classes.accessModifiers;

class NoModifierType {
    String name = "John";
    int age = 21;

     void display(){
         System.out.println("This is a no modifier type of class with no modifier of:" + name + " whose age is " + age);
    }
}
